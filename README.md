# qscmf-http-trace-logger

#### 安装
  
  ```php
  composer require quansitech/qscmf-http-trace-logger
  ```

#### 用法
+ 设置ENV
  ```ENV
  # 存储接口请求日志的数据表名，如 api_requests_log，必填  
  QSCMF_HTTP_TRACE_LOGGER_TABLE_NAME=
  ``` 
  